# Dependencies
node_modules/

# Environment / secrets
.env
dev.env
*.pem
client_id.json

# Generated assets (rebuilt at runtime)
static/beta/images/generated/
static/beta/videos/generated/
static/beta/docs/generated/
static/generated/
output/
scorecard-analysis/

# Logs
logs.log
*.log

# macOS
.DS_Store

# IDE
.vscode/launch.json
.vscode/settings.json

# One-off seed data (run manually, not from repo) — the schema migrations
# themselves belong in git, only the data/ subfolder is exempt
migrations/data/
scripts/

# Legacy / scratch files (never committed)
_amplience/
_tools/
_fordeletion__/
badwords.txt
demo.html
demo2.html
original-nginx.conf
player-registration-pseudo.txt
rootfiles/sw3.js
rootfiles/swv2.js

# Playwright (see playwright.config.js) — run artefacts, never committed
test-results/
playwright-report/
blob-report/
.last-run.json

# Claude Code: settings.json is shared (permissions the whole team benefits from);
# settings.local.json is personal and worktrees/ is scratch state.
.claude/settings.local.json
.claude/worktrees/
