# Hardening backlog

Work packages from the August 2026 audit. Each one is self-contained: an agent or a
person can pick a single file up cold, without the conversation that produced it.

**Read this file first**, then the package you have been assigned. Do not work on a
package that is not assigned to you — several of them touch the same files, and the
conflict map below is the only thing keeping that safe.

**Landed packages live in [`done/`](done/).** The top level is what is still open, so a
glance at the directory answers "what is left". They are kept rather than deleted because
each one records why the code is shaped the way it is, and the status table below still
links to them — worth reading before touching a file a finished package owned. No package
is currently in that state. (HARD-22 is human-only work too, but it has not started at all,
which is a different thing from finished code waiting on a switch.) One package was: HARD-12
sat at the top level with its code complete because a **human** still had to flip
`CSP_ENFORCE`, which happened on 11 Sep. Moving it into `done/` would have hidden the only
part still outstanding. HARD-02b and HARD-07 were the
other two until 1 Sep 2026, when the bucket steps and the audit scheduling were both
carried out; HARD-02b's one remaining *optional* extra became HARD-22 rather than keeping
a finished package open.

## Where the findings came from

Two passes, published as artifacts and rendered to PDF in `pdf/` (gitignored):

| Pass | Question | Artifact |
|---|---|---|
| One — security & operability | What could an attacker or an outage do, and what could a non-technical owner not fix? | [League Site Risk Register](https://claude.ai/code/artifact/509ed4e6-ba01-40b6-9e1f-0417fd1d5422) |
| Two — seasonal & functional | What breaks for captains and members during a season? | [What Breaks During a Season](https://claude.ai/code/artifact/e6b5419f-343a-4c69-b8b3-b0aa5818ff98) |

Regenerate the PDFs after editing either page:

```bash
node tools/artifact-to-pdf.js docs/hardening/pdf/risk-register.html \
                              docs/hardening/pdf/season-failures.html \
                              --out docs/hardening/pdf
```

## The tools you should be using

Do not hand-write database boilerplate. It was the single largest waste in the audit.

```bash
node tools/dbq.js "SELECT id, name FROM team LIMIT 5"   # ad-hoc read (refuses writes)
node tools/dbq.js --schema player                        # columns of a table
node tools/dbq.js --check all                            # every integrity check
node tools/dbq.js --check orphan-results                 # the offending rows
```

`tools/audit/checks.js` holds the checks. **Every package below that changes data or
data-handling must run `--check all` before and after, and report the difference.**

**`dbq.js` reads production.** `tools/lib/loadEnv.js` loads `.env` ahead of `dev.env` for
every tool, so an ad-hoc query answers about the live database; `--local` asks for the
development one deliberately. `dev.env` itself has pointed at a local Postgres in Docker
since HARD-13 — the tools do not follow it, on purpose, and that one-place decision exists
because it silently went wrong the day the local database arrived (dotenv does not
overwrite an already-set variable, so whichever file loads first wins, and every tool
loaded `dev.env` first — `--check all` ran for days against a two-year-old local seed).

`dbq.js` refuses anything that is not a single read. A write belongs in a reviewed script
under `scripts/` with a dry run, modelled on `scripts/backfill-contact-emails.js`. Those
scripts are gitignored and load `dev.env` FIRST, so a script written to fix production data
will target the LOCAL one — check the top of any script before trusting what it reports.

## Rules of engagement

1. **One package per agent.** Check the conflict map before starting.
2. **Every fix needs a test that fails without it.** The discipline that has worked on
   this codebase: write the test, stash the fix (`git stash push <files>`), confirm the
   test fails, `git stash pop`, confirm it passes. Three of this year's bugs lived
   behind a green suite because nobody did this.
3. **Run `npm test` before you claim done.** Roughly 1,170 tests across 85 suites, about
   a minute — but **take the exact numbers from the run, not from here**. A figure written
   down is wrong by the next package: this line has been stale three times now, and the
   `/hardening` skill's copy of it was deleted rather than corrected for exactly that
   reason. If you touched anything the browser drives, `npm run test:e2e` too (89 tests in
   12 spec files, one of which writes — deliberately and by name, see rule 6) — but see
   rule 7 if you are one of several agents.
4. **Read `CLAUDE.md` first.** It documents the Postgres quoting rules, the missing
   `insertId`, the canonical-URL trap and the roster rank convention. Most of the
   listed gotchas were expensive to learn.
5. **Stay in scope.** Each package has an *Out of scope* section. If you find something
   else, add it to this backlog rather than fixing it.
6. **Do not widen the Playwright read-only allowlist** (`e2e/helpers/read-only.js`).
   Since HARD-33 the browser suite runs against the local database with dead outbound
   credentials (`e2e/server-env.js`, which refuses to start if anything live survives), so
   this is no longer the last line of defence it once was — but it is still the thing that
   keeps "this spec writes" a statement a spec has to make. `scorecard-submit.spec.js` is
   the one that makes it, through `readOnly(..., { allowWrites })`. Name a write there;
   never widen `READ_ONLY_POSTS` to let one through.
7. **If you are working in parallel with other agents, do not run Playwright**, and do
   not trust a single loaded Jest run. Playwright starts its own dev server on a fixed
   port, so two agents racing it produce nonsense. Jest is safe per-worktree (a worktree
   contains no sibling worktrees), but concurrent Jest processes contend: that is what
   produced every "a different arbitrary subset failed" report on 31 Aug, all of them
   `Exceeded timeout of 5000 ms`. `testTimeout` is now 15s, which should absorb it. If a
   test fails on a busy machine, re-run that suite **alone** before reporting it — and if
   it fails with a wrong *status* rather than a timeout, that is a real bug, not
   contention. Whoever merges runs the browser suite once on the merged result.

## Conflict map

Packages in the same wave touch disjoint handlers and can run in parallel. Adding a
*new* route line to `routes/index.js` is append-only and merges cleanly; editing the
*same handler* does not.

**This table is about ownership and ordering, not progress.** *Blocked by* names the
package that must land first; whether something has landed is the status table's job, and
nine rows carrying `done` in this column is how the two came to disagree. Most rows here
are finished packages, kept because the *Owns* column is the record of which package owns
a file — worth reading before you edit one. **What is open is `ls docs/hardening/*.md`.**

| Package | Owns | Wave | Blocked by |
|---|---|---|---|
| [HARD-01](done/HARD-01-scorecard-submission.md) Scorecard submission integrity | `controllers/scorecardController.js` (`full_fixture_post`), `models/fixture.js` | A | — |
| [HARD-02](done/HARD-02-s3-upload-lockdown.md) S3 upload lockdown | `routes/index.js` (`/sign-s3`), new `utils/uploads.js` | A | — |
| [HARD-04](done/HARD-04-process-resilience.md) Crash handlers, shutdown, `/healthz` | `app.js` | A | — |
| [HARD-05](done/HARD-05-error-handling-sweep.md) `res.send(err)` sweep | `fixtureController`, `playerController`, `clubController`, `divisionController` | A | — |
| [HARD-08](done/HARD-08-tests-in-ci.md) Run the tests in CI | `cloudbuild.yaml` | A | — |
| [HARD-09](done/HARD-09-data-cleanup.md) Clean the known-bad data | `scripts/` + database | A | — |
| [HARD-03](done/HARD-03-photo-and-links.md) Photo endpoint + confirmation links | `controllers/scorecardController.js` (other handlers) | B | HARD-01 |
| [HARD-06](done/HARD-06-friendly-errors.md) Friendly 500 page | `views/500-error.ejs`, `routes/index.js` (error handlers) | B | — |
| [HARD-07](done/HARD-07-weekly-anomaly-email.md) Weekly anomaly email | new controller + view, one new route | B | HARD-09 |
| [HARD-10](done/HARD-10-team-lifecycle.md) Withdraw a team properly | `teamController`, `models/league.js`, admin views | B | — |
| [HARD-11](done/HARD-11-referential-integrity.md) 2,132 orphaned team references | `models/players.js`, `models/fixture.js`, `models/roster.js`, `models/club.js`, migration 017 | B | — |
| [HARD-02b](done/HARD-02b-private-scorecard-photos.md) Make scorecard photos private | `routes/index.js` (`/sign-s3`), a new read path | C | — |
| [HARD-12](done/HARD-12-security-headers.md) helmet + CSP | `app.js` | C | HARD-04 |
| [HARD-13](done/HARD-13-dev-database-guard.md) Stop dev pointing at production | `app.js`, `dev.env` | C | HARD-04 |
| [HARD-14](done/HARD-14-flaky-authorization-test.md) A flaky authorization test | `__tests__/integration/roster.test.js` | A | — |
| [HARD-16](done/HARD-16-reserve-rank-migration.md) Finish the reserve-rank migration | `scripts/` + database | A | — |
| [HARD-18](done/HARD-18-migration-runner.md) Migration runner splits on comment semicolons | `run-migration.js`, `utils/sqlScan.js` | A | — |
| [HARD-32](HARD-32-migration-tracking.md) Nothing records which migrations have run | `run-migration.js`, a new `schema_migrations` table | C | — |
| [HARD-17](done/HARD-17-draft-games-divergence.md) Draft and games disagree for 1 fixture in 9 | `controllers/scorecardController.js`, `models/fixture.js`, `views/scorecard-conflict.ejs`, migrations 015 + 016 | B | — |
| [HARD-15](HARD-15-inline-scripts.md) Remove the inline scripts so the CSP can bite | `views/**/*.ejs`, `static/beta/js/` | D | HARD-12 |
| [HARD-19](done/HARD-19-alias-and-join-guards.md) Guards for the two recurring gotchas | `__tests__/unit/`, plus whatever it finds | A | — |
| [HARD-20](done/HARD-20-phantom-401.md) A 401 from routes with no authorization | `__tests__/` setup | A | — |
| [HARD-21](done/HARD-21-social-video-read-proxy.md) The weekly social video has never worked, in three ways | `controllers/socialVideoController.js`, one new route | C | — |
| [HARD-22](HARD-22-lock-the-bucket.md) Stop the bucket drifting public again | AWS bucket settings only, no repo code | C | — (do HARD-21 first) |
| [HARD-23](done/HARD-23-invoice-automation-auth.md) Make.com cannot authenticate to the invoice endpoint | `routes/index.js` (`/league/sendInvoices`), `contactusController.js`, new `middleware/requireCronCaller.js`, `views/admin/invoices.ejs` | B | — |
| [HARD-24](done/HARD-24-unauthenticated-result-publish.md) `POST /scorecard-beta` publishes a result unauthenticated | `routes/index.js`, `scorecardController.js` (`full_fixture_post`), `views/populated-scorecard.ejs`, new `middleware/requirePublishAuthority.js` | B | — |
| [HARD-25](done/HARD-25-convert-document-scorecards.md) Convert document scorecards to images on the way in | `utils/uploads.js` or a new receive path | B | — |
| [HARD-28](done/HARD-28-scorecard-e2e-coverage.md) What the scorecard flows are supposed to do | `e2e/scorecard.spec.js`, `e2e/messer-scorecard.spec.js`, `e2e/populated-scorecard.spec.js` | A | — |
| [HARD-33](done/HARD-33-dev-env-live-credentials.md) The browser suite's dev server holds live AWS credentials | `e2e/server-env.js`, `playwright.config.js` | A | — |
| [HARD-36](done/HARD-36-failed-scorecard-analysis-is-unfalsifiable.md) A failed scorecard analysis destroys its own evidence | `controllers/scorecardAnalysisController.js` (`analyse_scorecard`), one S3 lifecycle rule | C | — (do with HARD-22, same bucket) |
| [HARD-37](HARD-37-social-card-artwork.md) The social artwork constrains every card drawn on it | `static/beta/images/bg/social-*.png`, `controllers/socialController.js`, `tools/artwork/` | D | — |
| [HARD-29](HARD-29-flatedecode-scorecards.md) `/FlateDecode` scorecards, the last 3% | `utils/documentImage.js` | B | — |
| [HARD-30](done/HARD-30-ccitt-scorecards.md) `/CCITTFaxDecode` scorecards, or the decision not to | `utils/documentImage.js` | B | — |
| [HARD-26](done/HARD-26-test-process-carries-production-secrets.md) The test suite runs with production credentials | `__tests__/setup.js`, the `dotenv` call in `app.js` / `instrument.js` | C | — |
| [HARD-27](done/HARD-27-getemails-sql-construction.md) `Player.getEmails` concatenates its SQL, including the PI key | `models/players.js` (`getEmails`, `getById`, `updateBulk`) | C | — |
| [HARD-31](done/HARD-31-pi-key-rotation-decision.md) Decide, knowingly, whether to rotate `DB_PI_KEY` | `scripts/hard31-rotate-pi-key.js` (gitignored), the database, Cloud Run | C | — |
| [HARD-34](HARD-34-scorecard-on-a-phone.md) Filing a result on a phone | `e2e/` (a new mobile spec), `views/index-scorecard.ejs`, `views/messer-scorecard.ejs` | A | — |
| [HARD-35](HARD-35-invoice-run-reporting.md) A scheduled send that did not happen should say so | `utils/auditDigest.js`, `auditController`, or a completion mail from `contactusController` | C | — |

## Priority

**This list is historical and is not a queue.** It ranked the top of the two original audit
passes, all four of which are long done, and it has deliberately not been renumbered as work
landed — renumbering a list like this is how it comes to be trusted after it has gone stale.
It is kept because the *reasoning* is still worth reading: why discovery outranks most
individual faults, and why HARD-26 had to precede HARD-08.

**What is open is what `ls docs/hardening/*.md` lists.** Landed packages move to `done/`, so
the directory listing is the backlog and the status table below is the current state. To
choose between what is left, the `/hardening` skill has the ranking questions worth asking;
the short version is that a finding with a dated incident behind it beats a hypothetical, a
fault on a path that is live this season beats one that costs something in a year, and a
package whose code is done but which still needs a human step is usually worth finishing
before starting another.

The original list, as written:

1. **HARD-01** — the only finding that has already lost league data. Three results from
   last season sit in the table with no games behind them.
2. **HARD-02** — the largest blast radius. Has never fired, which is the only reason it
   is not first.
3. **HARD-07 + HARD-04** — discovery. Every other finding is worse when the way you
   learn about it is a member emailing you.
4. **HARD-08** — 439 tests that only run when somebody remembers. **Read HARD-26 first**:
   the suite currently runs with production credentials, so putting it in CI as it stands
   means putting those credentials in CI. That is the one thing that promotes HARD-26 out
   of the long grass.
5. Everything else.

## Status

Update the table below as packages land, so a fresh session can see where things
stand without reading git log.

| Package | Status | Commit | Notes |
|---|---|---|---|
| SEC-3 | **done** | `f5f36ff` | Invoice endpoints gated to superadmin. Not a package — pulled forward because the annual send was the next day. |
| HARD-01 | **done** | `17e2d0e` | Transaction, resubmit page, deterministic lookup, 18-game validation. 17 new/updated tests. |
| HARD-02 | **done** | `8f1fb71` | Server-generated keys, image-only content types, rate limit. Residual tracked as HARD-02b. |
| HARD-03 | **done** | `01e10fe` | Photo URL validated + escaped, write gated, per-draft `confirmToken` in the confirmation link. `migrations/011_scorecard_confirm_token.sql` **applied 31 Aug 2026** (1557 rows unchanged, 0 tokens minted, so every confirmation link already in a captain's inbox still works). Tokenless drafts are grandfathered (see the clause in `utils/scorecardLinks.js`). |
| HARD-04 | **done** | `19d27ec` | unhandledRejection + uncaughtException handlers, SIGTERM drain, health endpoint above the limiter. **Monitor `/health`, not `/healthz`** — Google's frontend intercepts the exact literal path `/healthz` in front of Cloud Run and answers its own 404, so the request never reaches the container (found 31 Aug by curling production after the deploy; `/healthz/` and `/HEALTHZ` both return 200, which is how it was pinned down). The endpoint had been shipped, tested and documented at the one spelling that could not be reached. Both paths are now registered. |
| HARD-05 | **done** | `4a10c68` | All 11 replaced with next(err), plus a repo-level guard test. |
| HARD-06 | **done** | `869257e` | Error out of the page entirely, six-hex reference on the page and as a Sentry tag, three canonicals moved to `canonicalFor`. Left alone, same `req.get('host')` bug: `middleware/validateSeason.js:23` (its own 404 render) and `routes/index.js` lines 83 (`failed-login`) and 408 (`/user`) — out of this package's ownership. |
| [HARD-07](done/HARD-07-weekly-anomaly-email.md) | **done** | `932fde3` | `GET /admin/audit` (superadmin, renders the email itself so preview and inbox cannot diverge) and `POST /admin/audit/run` (superadmin session **or** `X-Audit-Token`, hashed then `timingSafeEqual`). **Configured 1 Sep 2026**: `AUDIT_EMAIL_TO` and `AUDIT_CRON_TOKEN` set on `league-site` in project **`stockport-badminton-map`** (europe-west2), and Cloud Scheduler job `sbl-weekly-audit` created for `0 8 * * MON` Europe/London, ENABLED. The Scheduler API had to be enabled first, and **`--update-env-vars` not `--set-env-vars`** — the latter replaces the whole environment and would drop `DATABASE_URL` and the Auth0 keys. **First send verified 1 Sep** — and it failed silently the first time, which is the lesson worth keeping: `AUDIT_EMAIL_TO` was typed `gmail.co`, and Cloud Scheduler showed green, the endpoint returned 200, the logs were clean and the controller reported `sent: true` — all *correct*, because SES accepted it and the bounce came back asynchronously where nothing watches. **The digest had, in its own delivery path, the exact failure it exists to eliminate.** `/admin/audit` cannot catch it either: it renders without sending. When the digest is silent, read the env var value before anything cleverer. Originally: **nothing sends until `AUDIT_EMAIL_TO` is set** — unconfigured, the job builds the digest, answers `{sent:false,reason}` and mails nobody, deliberately, because `dev.env` points at production. Also set `AUDIT_CRON_TOKEN` (unset closes the token path rather than opening it), then `gcloud scheduler jobs create http sbl-weekly-audit --schedule="0 8 * * MON" --time-zone="Europe/London" --uri="https://stockport-badminton.co.uk/admin/audit/run" --http-method=POST --headers="X-Audit-Token=<token>"`. The digest **suppresses detail, never findings** — a `TRACKED` baseline in `utils/auditDigest.js` collapses HARD-10's and HARD-11's rows to one line each with a reason, so the first email is `all clear — 3 known issues tracked` at 6KB instead of 2,134 table rows; growth in any of them escalates in full, shrinkage asks for the baseline to be lowered, and a baseline that stops finding anything asks to be deleted. No new table, no DB write. **Not** wired into the Admin nav (`views/nav.ejs` belongs to HARD-10's wave) and `AUDIT_EMAIL_TO`/`AUDIT_EMAIL_FROM`/`AUDIT_CRON_TOKEN` are **not** yet in CLAUDE.md's env list, for the same reason — both are one-liners for whoever merges. Separately: `npm test` collects **nothing at all** inside a worktree, because HARD-14's `testPathIgnorePatterns: /\.claude/` matches the worktree's own path; run `npx jest --testPathIgnorePatterns=/node_modules/` until that is anchored to the repo root. And a contended full run produced `friendly-500 › answers 500` → **401** on `GET /fixtures`, a route carrying no `checkJwt` — a wrong status, not a timeout, so per rule 7 it deserves a look alongside HARD-14's outstanding 403→404. |
| HARD-08 | **done** | `5d48fa6` | `npm ci && npm test` before the build. Verified the suite passes with no .env, as in CI. Playwright stays local — HARD-13. |
| HARD-09 | **done** | see below | `--check all` went from **8 of 11 failing to 3 of 11**. The three left were correct *at the time*: `orphan-team-refs` (2,132 — HARD-11, which needed a decision and got one on 14 Sep; now 0) and `short-squads`/`ghost-teams` (both Parrswood C, withdrawn 31 Aug — HARD-10). **Those numbers are a record of Aug 2026, not the current state** — there are 14 checks now, and the only honest answer is `node tools/dbq.js --check all`. Applied: 114 stale fixtures voided; fixture 345 corrected 2–14 → 4–14 from its own game rows; 72 null ranks normalised across 10 lists via `Roster.renumberGender`. **Not** applied, by decision: the 4 orphaned results (not reconstructable — see HARD-17) and 7 unrecoverable bad totals; both are now excluded by id in `tools/audit/checks.js` with the reason beside them, so they cannot mask a new one. Two checks were themselves broken and are fixed — see the note in that file. |
| HARD-10 | **done** | `8e7d79b` | Withdraw and reinstate a team from `/admin/teams`, superadmin only. The team row is never deleted (that is how the 2,132 orphaned refs happened); its division is cleared and kept in `"withdrawnDivision"`. Outstanding fixtures are **voided, not conceded** — conceding invents 18–0 results against opponents who never played, handing out points on the strength of fixture-list order. Recorded results are never touched, the predicate is repeated in the UPDATE's WHERE, and `"withdrawnFixtures"` stores the exact ids voided so Reinstate cannot resurrect the 114 that HARD-09 voided. `migrations/012_team_withdrawal.sql` **applied 1 Sep 2026** — 36 teams unchanged, 0 withdrawn, `--check all` identical either side. Checked against HARD-18: it splits into five chunks, none starting with prose. |
| HARD-12 | **done — enforcing since 11 Sep** | `aa20cf8`, `eb87d4f`, `9e6f2df` | helmet + two CSP headers: an enforcing baseline with no resource allowlist, and the full allowlist report-only. `CSP_ENFORCE=true` flips it — prerequisites in `utils/securityHeaders.js`, do not flip without them. Nothing was receiving reports (Sentry needs setup that was never done), so `POST /csp-report` collects them into Cloud Logging. Residual: `'unsafe-inline'` must stay in `script-src` until the 159 inline `onclick` handlers go — tracked as HARD-15.  **2 Sep: 94 reports in 2 days and the flip is blocked on real breakage, not on waiting.** `CSP_ENFORCE=true npx playwright test` gives **43 passed / 5 failed**, all scorecard, and two are *functional* — the team and player dropdowns never populate, so captains could not file results. Cause: **`views/index-scorecard.ejs` loads `/scripts/ejs/ejs.js`, i.e. EJS in the browser**, which compiles templates with `new Function` and so needs `'unsafe-eval'`. Adding it is one line but, next to the `'unsafe-inline'` that must stay, would leave `script-src` constraining only *where* scripts load from — so it is a decision, not a fix. Also blocked: `worker-src blob:` (Sentry Replay's compression worker, path-gated hence scorecard-only) and three missing `connect-src` hosts (doubleclick, google.co.uk, connect.facebook.net) which should be added regardless. **Prerequisite 3 is entirely unmet** — none of `/admin`, `/player-stats`, `/pair-stats`, `/file-upload`, `/event/`, `/clubs/`, `/contact-us` has reported, so six third-party libraries remain untested. **Resolved later that day.** The three `connect-src` hosts and `worker-src blob:` went in (`eb87d4f`), and the `unsafe-eval` requirement was **removed rather than conceded** (`9e6f2df`): client-side EJS is replaced by `static/beta/js/form-options.js`, which builds the `<option>` lists with DOM calls. `CSP_ENFORCE=true npx playwright test` now gives **48 passed, zero violations**, up from 43/5. `script-src` never had `'unsafe-eval'` — the policy was right and the site was wrong, which is worth knowing because adding the directive was the one-line temptation. **8 Sep: prerequisites 2 and 3 are all but met — re-measured, not assumed.** Counting only Cloud Logging reports later than the last policy change (`e2eb60e`, 3 Sep 23:23), because an earlier report says nothing about the policy as it stands: **5 reports in 5 days, all one host**. `connect-src https://analytics.google.com` — the list had `https://*.analytics.google.com`, and a CSP wildcard matches subdomains but **not the domain itself**, which gtag also fetches. Fixed with the bare host and a test (`__tests__/integration/security-headers.test.js`), since a wildcard sitting next to it reads as though it already covers this. Analytics only, so it broke nothing and the report was the only evidence it existed. **Prerequisite 2 is met in substance**: between 4 and 8 Sep a captain ran the whole chain for real — wizard, draft, `POST /scorecard-beta`, confirmation, and a presigned S3 photo upload (`GET /sign-s3` -> `POST /add-scorecard-photo`) — with no violation at all. That upload is the path most likely to break and **Playwright cannot cover it**, because the browser suite is read-only and never performs the PUT. **Prerequisite 3 is down to one page**: `/event/` (351 hits), `/contact-us` (48) and `/clubs/` (28) are clean on real traffic; `/player-stats` and `/pair-stats` were verified 3 Sep and the only change since was additive to `style-src`. Left: **Quill on `/admin/homepage-content/<id>`**, which is the page that actually reported on 3 Sep (`style-src-elem` -> `cdn.quilljs.com`) and has had **zero requests** since the fix — untested, not clean, which is exactly the ambiguity the prerequisite warns about. **FLIPPED 11 Sep 2026 — `CSP_ENFORCE=true` is set on the Cloud Run service, and the resource allowlist is now enforcing.** The allowlist moved out of `Content-Security-Policy-Report-Only` and into `Content-Security-Policy`; the report-only header is gone and `report-uri /csp-report` survives on the enforcing one, so a blocked resource is still visible. Re-verified on the morning of the flip rather than trusting the previous day's answer, because three templates had changed since: **zero violations** in the whole report-only period after the 8 Sep analytics fix — a window that now includes both league nights and a real result publish — and `CSP_ENFORCE=true npx playwright test` at **71/71**. That re-run earned its place: it caught a test stubbing the S3 upload on an invented `bucket.invalid` host, which `connect-src` refuses — it passed report-only and failed enforced, i.e. it was exercising a path the browser will never take (`0e65a96`). Verified immediately after the flip in a real browser against production: eleven public pages, **zero `securitypolicyviolation` events**. Rollback is one redeploy: `--remove-env-vars CSP_ENFORCE`. Residual unchanged — `script-src` keeps `'unsafe-inline'` while the 159 inline `onclick` handlers remain, so this is defence in depth and not an XSS fix; that is HARD-15. |
| [HARD-13](done/HARD-13-dev-database-guard.md) | **done** | `3cbb448` | Option 1 AND option 3 from the brief, because option 1 turned out to be cheap: the schema (`migrations/001_initial.sql`, 60 tables — 12 live, 48 season archives) and a 5.3MB seed were already on disk, so `tools/local-db.sh up && load` rebuilds from nothing in ~5 seconds. Contact details are **replaced, not copied** — `bigcoops+firstnamelastname@gmail.com` and Ofcom's reserved `07700 900xxx` range, re-encrypted under a local key — so a dev box holds no member's details and any mail a dev run sends lands in one inbox. `dev-fixtures.sql` adds what a two-year-old snapshot cannot: current-season fixtures, a draft with a `confirmToken`, a messer draft. `ANALYZE` runs at the end, without which the planner sequential-scans 35,000 games and the slowness reads as test flakiness. The guard (`utils/devDatabaseGuard.js`) sits inside `if (require.main === module)` so the 35 suites requiring `app.js` are untouched — asserted by a test, since moving it to import time would break every one. All three acceptance criteria met, including the one I nearly shipped unmet: the override is now **announced in a banner at boot**, because an escape hatch nobody can see they are standing on is the original hazard with extra steps. Three unrelated things it flushed out, all fixed in the same commit: the browser suite exhausting the sitewide 600/15min rate limit (every page after that a 429 that renders without the elements the tests look for — it names a missing locator and says nothing about a limit), a spec hardcoding a season that is *past* in production and *current* locally, and two `console.log(renderData)` dumps putting every player name into Cloud Logging on each confirmation-page view. |
| HARD-14 | **superseded 7 Sep** — see the combined HARD-14 + HARD-20 row below | `d60312d` | **Most of what looked like flakiness was contention, not the suite.** Three agents working in parallel worktrees each ran their own Jest, and the reports that came back ("`sign-s3`, `club-pages`, `roster`, `event-page-and-sitemap` fail intermittently, a different set each run") all describe the same mechanism: the default 5s `testTimeout` is too tight for a supertest case that boots the whole app once the machine is contended. Measured, not assumed — the suite is **12/12 clean** alone, clean at **load 12–14**, and clean at `--maxWorkers=16` on 8 cores; it takes **two concurrent Jest processes** to fail one arbitrary test, and the failure is always `Exceeded timeout of 5000 ms`. `testTimeout` is now 15s. Separately, a run in the main checkout was collecting **140 test files, 106 of them from `.claude/worktrees/`**, because `testMatch` starts with `**` — now ignored. **Update, 1 Sep:** one of the intermittent failures turned out to be a *test*, not contention — the `HSTS_INCLUDE_SUBDOMAINS` case did `jest.resetModules()` + `require('../../app')`, building a second app and a second pg pool, and Jest reported a worker failing to exit gracefully. It failed about one full run in nine and was twice written off as contention. Fixed by moving the config into `utils/securityHeaders.js` so it can be read without booting anything. **The reasoning that missed it is worth naming: "did not reproduce in N runs" is not evidence of contention, it is absence of evidence.** A wrong *status* is never contention — contention produces timeouts. **Two wrong-status failures are still open**, and both fail in the permissive direction, which is why they are not dismissed as contention: `friendly-500 › answers 500` returned **401** on `GET /fixtures` under a contended run (a route carrying no `checkJwt` at all — found by HARD-07's agent, 31 Aug), and the original 403→404 on `roster.test.js` › *403s a club admin ordering another club's team*. That is a wrong *status*, not a timeout, so it is a different bug and the one that still matters — a test that fails in the "access granted" direction. Reproduce it alone before assuming it is contention too. |
| [HARD-02b](done/HARD-02b-private-scorecard-photos.md) | **done** | `8312d1d`, `2a2589a`, `7d67e7d` | `GET /scorecard-photo/:id` serves photos through the site; `/sign-s3` no longer asks for `ACL: public-read`. **The bucket-level steps are deliberately not done** — objects keep the ACL they were written with and a public-read bucket policy would override the missing ACL anyway; the ordered runbook and how to reverse each step is in the package file. Two regressions caught by Playwright *after* merging, both invisible to unit tests that invent their own URLs: stored URLs spell a space as `+` (the old widget rewrote `%20`), and `GetObject` takes the key literally, so **every historical photo 404'd**; and 109 of the 1,479 scorecards on record are PDFs or Word documents, which the image-only proxy dropped — 7% of the archive. Both fixed, with tests taken from the real column. **Step 0 is now measured, not spot-checked** (`tools/scorecard-photo-audit.js`, read-only): **1,456 of 1,479 rows servable, and 0 refused by the guard** — so every stored URL shape resolves and step 3 cannot blank a photo that works today. The 23 failures already 404: 21 absent objects (17 of them the contiguous block ids 878–900, all Feb 2020) and 2 keys with no real extension. Also found: **406 bucket objects no row references**, and **step 4's warning is out of date** — `venues-map.png` and the weekly videos are already 403 to anonymous, the map being proxied with credentials. Five public objects are captain uploads that were never photos (`.xlsx`, `.msg`, a 25MB `.zip`, two extensionless). **Step 2 needs no action** — the bucket policy grants no public read at all (its one statement is `AllowSESReceiptWrite` into `inbound-email/*`), so per-object ACLs are the only lever and nothing overrides them. **Step 3 is unblocked and sweeps the whole root**: the Tameside league shares this bucket (338 root objects, all `tameside-`-prefixed) and has now shipped its own read proxy and dropped `public-read` from its `/sign-s3`, so no carve-out is needed — their reply, with its evidence, is copied to `docs/handover/tameside-s3-bucket-reply.md`. **Their one ask: do not switch Object Ownership to `bucket owner enforced` without telling them** — it is a *write* dependency (a presigned PUT carrying `x-amz-acl` fails), safe now that neither side sends one, but done in the wrong order it breaks uploads rather than displays.  **Step 3 ran 1 Sep 2026: 1,454 root objects made private, 0 failures.** The audit is byte-identical either side (1,456 servable) because the proxy reads with credentials; anonymous sampling went from 100% public to 0. Reverse with `scripts/hard02b-step3-acl-sweep.js --rollback --apply`, which restores exactly the snapshot's public set rather than everything — the distinction matters because `venues-map.png` was the one object already private. `scripts/` is gitignored so that snapshot is on one machine, but the rollback is reconstructable: every root object was public except the map. Step 4 was **pulled out to HARD-22** rather than keeping a finished package open on an optional extra. |
| [HARD-15](HARD-15-inline-scripts.md) | not started | | Residual from HARD-12. **162 inline handlers — 159 of them `onclick=`**, the other three an `onsubmit`, an `onerror` and an `onchange` — plus **34 inline `<script>` blocks across 12 templates** (the "18 templates" recorded here until 15 Sep was neither figure; re-measure rather than trust this line: `grep -rhoE '\son[a-z]+=' views --include='*.ejs' \| wc -l`). So `script-src` must keep `'unsafe-inline'` — the CSP stops a script being *loaded* from an unlisted host but not an injected inline one. **A nonce is not a shortcut here: it makes the browser ignore `'unsafe-inline'` and cannot attach to an `onclick` at all.** |
| [HARD-11](done/HARD-11-referential-integrity.md) | **done 14 Sep** | `2ad93ad` | The survey's answer was far more concentrated, and far larger, than the brief guessed. Seven queries join `fixture` to `team`; six are fine (they read current-season data and **2026 has zero orphans** — the last source, `rearrangeByTeamNames` inserting NULL-team fixtures, was closed in September). The seventh is **a player's match history, and it was dropping 10,422 of 35,244 game rows — 30% of everything ever recorded — affecting 677 of 904 players.** Susan Forbes played 716 rated games and her page showed 256. It now shows 716. Converted with `LEFT JOIN` + `COALESCE(…, 'Former team')`; the team **rank** is deliberately left NULL rather than coalesced, because it feeds `teamAdjustment` and the view guards `> 0` — a substituted number would invent an adjustment that never happened. Guarded by `__tests__/unit/player-history-join.test.js`, 3 of whose 4 assertions fail without it. **The sitemap was left alone on purpose and it is not an oversight.** It drops 14 URLs, correctly: those `/event/:id` pages return no rows at all, because `getFixtureEventById` inner-joins both teams as well. Fixing the sitemap alone would put 14 soft-404s in it, which the `seo` skill forbids in terms. Making those pages render is a decision rather than a conversion — should a fixture whose team was deleted have a public indexable page, rendering with no venue, division or club? — and it touches JSON-LD. That belongs with step 3. Also noted: **9 of the 19 orphaned team ids still have their real names in the season archives** (15 = Disley A, 21 = Bramhall Village A, 34 = Macclesfield B). Not used, because reading them means hardcoding a list of archive tables that goes stale the moment a season is archived with nothing to catch it. **Steps 3 and 4 followed the same day.** `--check orphan-team-refs` **2132 → 0**: the 19 deleted teams reinstated as withdrawn rows on club 63 with a NULL division (HARD-10's convention), 9 named from the season archives and 9 by the league secretary from old correspondence, with 58 left as "Unknown team" — 8 fixtures in 2012-13 and no game rows, so nothing names it. Then `migrations/017`, **NO ACTION** foreign keys on both columns, confirmed enforcing against production inside a rolled-back transaction: deleting a team that has fixtures now raises `violates foreign key constraint`. Not CASCADE, emphatically — that would delete the fixtures and with them every player's record of having played them. **Five queries had to learn about `withdrawn` first, and one was load-bearing**: `findTeamIdsByName` keys a Map on team name, which silently keeps the last row for a duplicate — and four of these names are already held by a live team, so without the filter a rearrangement could have resolved to the 2014 team and written a fixture pointing at it, creating a new orphan by the very act meant to stop them. The others were the roster's "Move to…" list (a captain could have moved a live player onto a team that folded in 2014), the club picker's counts, the club page listing, and the reminder recipients. All latent, because nobody had ever used the withdrawal flow. **Public pages deliberately not granted** (owner's call): reinstating would otherwise have given ~2,132 fixtures an `/event/` page with no club, division or venue, and put 14 in the sitemap. Both queries exclude withdrawn teams and must agree, since a sitemap entry whose page does not render is a soft 404. **Both rehearsals earned their keep**: migration 017 correctly FAILED locally (that seed still has orphans — a database with orphans cannot take the constraint, which is the rehearsal), and the reinstatement script failed on `venue` being NOT NULL because its column list was written from what the query needed rather than what the table demands. |
| [HARD-16](done/HARD-16-reserve-rank-migration.md) | **done 14 Sep** | | 10 lists, **54 ranks written**, acceptance query 63 → **0**. Verified against a full before/after snapshot of all 582 ranked players rather than just the acceptance query, because "nobody changes section" and "nominated order unchanged" are claims about every row: **0 section changes, 0 lists whose nominated order moved, 0 left unranked**, `--check all` identical. Written through `Roster.renumberGender` per list in its own transaction — the tested editor path — never hand-written SQL. **The brief's figures were stale and that is the system working**: 162 players / 25 lists on 31 Aug, 63 / 10 by the time it ran. A list normalises as a side effect of its next save, the season started, captains have been saving. Its own *caution about timing* predicted this; re-measure before quoting a figure from a package that has been sitting. **It was two lines of SQL, not the one the brief promised.** Widening the `FILTER (WHERE rank < 99)` clauses was the first. The second: the selection **inner-joined `team`**, and 34 players point at a team id that no longer exists — 20 of them on a shared rank 99 — so they were dropped silently and the acceptance query, which does not exclude deleted teams, **could never have reached zero**. HARD-09's original had the same join and skipped them identically. `club` was already a LEFT JOIN, so it was an inconsistency as much as a bug. Gotcha 1c in a script, where `optional-join-guard.test.js` cannot see it because `scripts/` is gitignored. Found by rehearsing locally and re-running the acceptance query rather than trusting the run's own "54 written" — the run was honest, the selection was not complete. |
| [HARD-17](done/HARD-17-draft-games-divergence.md) | **done 14 Sep** | `8e2c067` | **Migrations 015 and 016 applied to production 14 Sep 2026, both before the code that writes them was deployed** — that ordering is not optional and 015's header originally claimed otherwise (kept in the header as a note, because a migration header is exactly what gets followed without checking). `Fixture.updateById` and `createScorecard` both build their statements from an object's keys, so a missing column throws, rolls back, and hands the captain the 500 page whose whole message is that nothing was recorded. **Provenance recorded, divergence deliberately not 'fixed'.** The diagnosis stands: results are corrected at validation, `game` is rewritten, the draft is left as filed, so the two records diverge because only one is ever corrected. Not corruption, no data fix. What is gone is the *guessing*: `migrations/015_fixture_draft_id.sql` adds `fixture."draftId"` and the publish records it, so nothing has to pair a fixture with its draft by team ids plus a ±3-day window any more — a window that, for the **140 of 1,373 matches with more than one draft**, returned a shortlist rather than an answer. **HARD-24 is what made this cheap.** The draft id was already in the publish request, because HARD-24 put it in the form so the token gate could check it; recording provenance became one key on `fixtureObject` rather than a research problem. **Migration 015 must be applied BEFORE this deploys**, and an earlier draft of its header said the opposite. `updateById` builds its SET clause from the object's keys, so a publish carrying a draft id issues `UPDATE fixture SET "draftId" = ?`; without the column that throws, rolls back, and hands the captain the 500 page whose whole message is that nothing was recorded. The wrong claim is kept in the header as a note, because a migration header is exactly the kind of instruction that gets followed without checking. Rehearsed with `--local`. **Second item done 14 Sep: a later submission supersedes an earlier one.** Put to the league secretary rather than guessed, because re-filing is the correction route the site has always had and a guard treating the second draft as an error would have broken it mid-season. Decision: the later filing wins. `migrations/016` adds `scorecardstore."supersededBy"`; NULL is "current", and earlier drafts point at the **newest** rather than their immediate successor, so finding the current record is one hop from anywhere in a chain. Nothing is deleted — the superseded rows are what a captain actually filed and when. Matched on exact pairing and date, **not** the ±3-day window the audit checks use: superseding the wrong draft would hide a real scorecard, so the safe direction is to supersede too few. **The teeth are on the publish side.** The earlier confirmation email is still in an inbox with a valid token, so without a check the *older* version publishes and the correction is what gets lost — the reverse of what filing again is for. It now answers 409, and the page deliberately does not link to the replacement, whose own token the old link was never granted. Superseding runs through `afterCommit`: it is bookkeeping about other rows after the captain's draft is already saved, and must not be able to reject the write it reports. Real SQL exercised against Postgres and rolled back, not just the mock. 1158 Jest, 89 Playwright. Measurement re-run against production per the acceptance criteria: **101/120 exact, 19 mismatches, 11 clean-profile**, up from 99/120 on 3 Sep, and no current-season fixture among the mismatches — new submissions are not adding to it. Note the script loads `dev.env` first and so reads the LOCAL database by default; run it with `.env` loaded first or the number is not comparable. 1151 Jest, 89 Playwright. | 
| [HARD-20](done/HARD-20-phantom-401.md) | **superseded 7 Sep** — see the combined HARD-14 + HARD-20 row below | | **The responses are not ours.** A captured failure body read `WebSockets request was expected` — a string absent from this repo and `node_modules`. Sweeping every HTTP-speaking listener inside macOS's ephemeral range (49152–65535) found **VS Code helpers on 49436 → 400, 49447 → **401**, and 51373/52383/54987 → **404**, plus Postman on 55773 → 404. Those are exactly the three phantom statuses recorded here and in HARD-14, and nothing else in the range speaks HTTP. `request(app)` binds a fresh ephemeral port per call, ~1000 times a full run; six conflicting ports in a 16,384 range gives ~30% per run, matching the observed rate and the "worse under load, never alone" behaviour. Explains every symptom: no 401 in the code, zero in production, always a wrong *status* not a timeout, and failures on docs-only trees. **HARD-14's residual 403→404 is the same thing** and needs no separate investigation. Fix is optional and about legibility: make the harness recognise a response the app did not produce and say so. Original note: **the symptom is a wrong *status*, not specifically a 401.** A third sighting was `mail-relay › 400s without a fixture to identify` returning **404** on `POST /fixture/reminder`, so the `express-jwt` theory cannot explain it; with HARD-14's 403→404 that is two of three non-401 sightings landing on 404. One session saw three failing full runs, **two of them on a docs-only working tree**, so it is not sensitive to the code under test. **Capture the output** — `npx jest \| tail` discards the failing test name and expected/received, which is how two of those three were lost. Originally: two tests seen returning **401 on routes that have no `checkJwt`** — `POST /contact-us` and `GET /fixtures`. There is no 401 anywhere in this codebase; `express-jwt` is the only thing that can produce one. **Production shows zero 401s in 30 days** (the same query returns plenty of 404s, so logging works), so this is confined to the test harness and is not an authorization bug — which is why it is low severity and why the investigation starts at the Jest setup. Rare: not reproduced in 19 consecutive full runs with a diagnostic in place. The brief carries the diagnostic to re-apply and the Cloud Logging query. |
| [HARD-19](done/HARD-19-alias-and-join-guards.md) | **done 7 Sep** (both halves) | | Written 1 Sep after the club contact page turned up **gotcha 1 and gotcha 1c for the second time each**, in a query edited four days earlier without either being spotted. Documentation has not stopped them; `no-res-send-err.test.js` did stop its equivalent. The brief carries the measurement that matters: a naive grep yields **43 candidates and 3 of 4 spot checks were false positives**, so the useful version has to get quoting right, ignore matches inside SQL strings, and tie an alias to the function that declares it. If it cannot be made precise, closing the package is a legitimate answer. |
| [HARD-21](done/HARD-21-social-video-read-proxy.md) | **done 21 Sep** | | Re-briefed rather than built as written: the original ask — hand Make.com a fetchable URL — solved a problem that no longer existed. Rebuilt so the video posts to Meta itself. **The feature had never worked in any respect, and not one fault was visible without running it.** A stale lock had it deadlocked for 115 days (the branch RECOGNISED the stale lock and never deleted it, so the atomic `IfNoneMatch: '*'` create always failed and blamed a concurrent run that did not exist). The slides were stretched, not letterboxed — `-resize 1920:1080` is an aspect RATIO in ImageMagick geometry, and `-extent` preceded `-gravity`/`-background` so the pad would have been NorthWest and WHITE; **the two hid each other**, so fixing the obvious one alone ships something worse. The bucket URL 403'd. The ffmpeg trigger was open to the internet. Phase 2 then killed three assumptions this package had written down as needing answers: Reels takes 16:9, 1:1 AND 4:5, and a silent audio track is fine — `FINISHED` means Meta will publish it, NOT that it looks good, which is the one thing a container cannot tell you. Ships at 4:5 to both platforms, matching the 1080x1350 cards so there are no bars at all. A stale render is REFUSED with a 409 rather than published as this week's results. **The live run then published successfully and reported failure**: Firebase Hosting cuts at 60s, the handler takes ~91s waiting on Meta to transcode, so Cloud Scheduler saw an error while Cloud Run logged 200 — gotcha 1bc, and a retry would have double-posted. That job now calls Cloud Run directly. |
| [HARD-22](HARD-22-lock-the-bucket.md) | not started | | Was HARD-02b step 4. Step 3 made 1,454 objects private one ACL at a time, which fixes the objects that existed and nothing else; **Object Ownership = `bucket owner enforced` makes `public-read` unexpressible rather than merely unused**, which is the durable version. Optional — the bucket is private today. Preconditions all verified 1 Sep and listed in the brief: no writer in either repo sends an ACL, and every reader is credentialed **except the weekly videos, which is why HARD-21 should go first**. Two traps: `bucket owner enforced` discards ACLs, so HARD-02b's `--rollback` stops being meaningful once it lands; and **the inbound-mail chain was baselined healthy on 1 Sep before any of this ran** (`tools/check-inbound-email.js --watch`, object landed and mail arrived), so a failure afterwards is unambiguously the lockdown's. Also **tell the Tameside side before flipping it** — it is a write dependency, and in the wrong order it breaks their uploads rather than anyone's displays. |
| [HARD-23](done/HARD-23-invoice-automation-auth.md) | **done 13 Sep** | `9272a6e` | **Make.com was dropped for this, not reconfigured.** The logs settled it: over the retained window Make makes 40-odd GETs — result images, league tables, tournament cards, outstanding fixtures — which is its real job, because it holds the social credentials. `POST /league/sendInvoices` appears **once**, on 1 Sep, and it uses nothing from the response. It was a clock with extra steps, and its clock is the bug: Make's HTTP module treats a 302 as success, which is the first of the three things that made 2026 invisible. Cloud Scheduler records a non-2xx as a failed run, and two jobs (`sbl-weekly-audit`, `sbl-daily-registrations`) already use exactly this shape. **Configured 13 Sep**: `INVOICE_CRON_TOKEN` set on `league-site` with `--update-env-vars` (**not** `--set-env-vars`, which replaces the whole environment and would drop `DATABASE_URL` and the Auth0 keys — verified afterwards that all 27 other variables survived), and Cloud Scheduler job `sbl-annual-invoices` created for `0 8 1 9 *` Europe/London, ENABLED, POSTing to the custom domain with `X-Invoice-Token`. `cloudbuild.yaml`'s deploy step passes no env vars, so the variable survives future deploys. **Retries are off** (`maxRetryDuration: 0s`, no `retryCount`): the run is not idempotent — it mails every club each time — and with a ±3 day window a next-morning retry would send duplicates. Recovery is Admin → Annual Invoices, which is what that page is for. **Verified against production, which is the only reason this is closed.** The job was triggered once by hand on 13 Sep, 12 days past the annual date, so it authenticated and then hit the date guard: `POST /league/sendInvoices` `status: 409`, `userAgent: Google-Cloud-Scheduler`, `commit-sha: 9272a6e`, **nothing sent**. That one line distinguishes the three outcomes that matter — 403 would mean the token never matched, 302 would mean nothing had actually changed, 409 means the whole chain works: Scheduler → custom domain → Cloud Run → env var → gate → date guard. A fix nobody has watched work is how `/healthz` shipped tested, documented, and at the one spelling production could not reach (HARD-04), which is why this stayed open until the log line existed. The Make.com scenario has been deleted, so nothing fires alongside it. **The gate is now shared, not copied.** `requireAuditCaller` and the registration equivalent were near-identical duplicates differing only in an env var, a header and a message; the brief said to reuse it and this is the third caller, which is what CLAUDE.md means by the second caller turning a fix into a rule. `middleware/requireCronCaller.js` is that gate once: token, then superadmin session, then **403 — never a redirect**, because `secured`'s 302 to `/login` is precisely what Make.com followed to Auth0, got a 200 from, and recorded as a successful job. Both existing callers were rewired onto it and their 37 tests passed unchanged, which is what makes the extraction believable. **`INVOICE_CRON_TOKEN` was added to all three places the other two tokens are known** — `__tests__/setup.js` (deleted), `e2e/server-env.js` (deleted) and `utils/testEnvGuard.js` (refuses the run if set). Missing any one of those is how the guard goes one variable out of date, which is the failure HARD-26 was written about. **The refusals are non-2xx now.** `res.send(["not the right date for invoices"])` was a **200 whose body is the error**, returned from *inside* the club loop after the first club had been queued; a caller that checks only the status — which Make.com does — could not tell a refusal from a run. Hoisted, and both refusals (wrong date, nothing to invoice) answer 409 with a line saying why. The window is now ±3 days rather than requiring today to *be* 1 September, and a **superadmin session** may override it outright; a **scheduler token may not**, because a scheduled run firing on the wrong day is a misconfiguration worth seeing. **Admin → Annual Invoices** is the button that did not exist: `grep -rn "sendInvoice" views/` returned nothing, so the recovery on the night was a `fetch()` in devtools. It shows where today sits relative to the annual date, lists the per-club outcome (a club that could not be invoiced appears in the same list as the successes, not as an absence), and requires typing SEND — deliberate friction, because that evening's corrected invoices went out *behind* the £NaN ones and the treasurers had to be told about the duplicate. `runInvoiceSend` returns `{status, lines}` so the page and the JSON endpoint cannot drift. Both halves demonstrated by reverting: restoring SEC-3's `secured` + `requireSuperAdmin` fails the 3 scheduler-token tests, and restoring the 200-refusal fails the 3 date tests. 1130 Jest, 89 Playwright. **Not done, and recorded rather than dropped:** step 5 of the brief — reporting the run into HARD-07's weekly digest — is in *What to do* and not in the acceptance criteria, the same shape as HARD-28's gap 6. It is now HARD-35. |
| [HARD-24](done/HARD-24-unauthenticated-result-publish.md) | **done 12 Sep** | | **The callers were checked first**, which is the whole discipline this package carried over from HARD-23, and the answer had changed since the brief was written: it recorded zero POSTs because the season had not started. Over the full retained window (17 May onward) there are now **5 POSTs to `/scorecard-beta`, all 200, all one desktop browser; 5 POSTs to `/email-scorecard`, each preceding a publish the same day; and 5 complete fixtures in the database, each with exactly 18 games** — three independent lines agreeing on one human in a browser, 1:1 with the drafts, no automation and no non-browser user agent. Confirmed with the owner that it is him, as superadmin. **The brief contained a trap and following it literally would have shipped a no-op.** It said to check the token with `mayOpenDraft` "exactly as the confirmation page does" — but `mayOpenDraft` grandfathers a *tokenless* draft OPEN (HARD-03, so links already in captains' inboxes keep working), and **1,557 of 1,562 drafts are tokenless**. A publish gate built on it would have admitted anonymous publishes for 99.7% of drafts while looking, in review and in a passing test, exactly like a fix. The brief's own next sentence assumed the opposite. So there are now two rules side by side in `utils/scorecardLinks.js`: `mayOpenDraft` (reading, grandfathered) and **`mayPublishDraft`** (publishing, never grandfathered), each carrying the note that says why it is not the other one. `middleware/requirePublishAuthority.js` takes a superadmin session **or** a valid token for the draft being published, and is deliberately NOT `secured`: that would admit any logged-in member *and* break the secretary's session-less email-link flow at the worst possible moment. It is mounted **before** validation so a refusal costs no fixture lookups. The form carries `draftId` and `t` as hidden fields; a tokenless draft renders an empty token and is superadmin-only, which is how all 1,557 of them were published anyway. **The "website updated" recipient is now derived server-side** from the stored draft row instead of `req.body.email` — the `/fixture/reminder` open-relay shape. Behaviour-preserving rather than a redesign: the hidden field was already rendered *from that same row*, so the legitimate submission was posting the address back to us and nothing changes about who is written to. **21 existing tests went 403 the moment the gate landed** — every one of them had been publishing a result with no authorization at all, which is a fair summary of the finding. They are now authorized through a new pattern this repo did not have: mocking `passport.session()` rather than `middleware/secured`, because an unsecured route never runs the latter. Both halves demonstrated by removal — 7 authorization tests fail without the gate, 2 recipient tests fail without the recipient fix, 2 render tests fail without the hidden fields. 1115 Jest, 89 Playwright, `--check all` unchanged (no data written). **Not covered by a browser test, deliberately**: publishing writes `fixture` and `game` and flips the fixture to `complete`, so a spec doing it could not be re-run without `local-db.sh load` — and CLAUDE.md is explicit that a spec which only passes against a freshly loaded database is one that gets skipped. |
| [HARD-25](done/HARD-25-convert-document-scorecards.md) | **done** | `d36124f`, `66e72a3`, `0051a83` | **Every document scorecard is an image inside a wrapper**, measured on six real files (smallest, median and largest of each type): a `.docx` is `word/media/image1.*` with **0 words** of text, a `.pdf` is **1 page, 1 image XObject, 0 fonts**. So converting them is *extraction, not rendering* — which is why it is cheap. Proven end to end with **no new dependencies**: docx via a zip read (verbatim bytes, no re-encode), `/DCTDecode` PDFs verbatim (the stream IS a jpeg), `/FlateDecode` via `zlib` + `sharp` (both already deps) — 3.00 bytes/px clean RGB, output checked by eye. **Ghostscript is not needed**, which matters because ImageMagick is in the image but delegates PDF rasterising to gs, which is not. 127 pdf + 14 docx = ~7% of the archive, 29KB to 20.7MB, and at least one is a *blank* form. The point: this makes "tell captains to stop using documents" unnecessary — accept what they send, store an image, and the OCR reader can then read it too. **Planned 4 Sep with measured numbers.** Codec split over 30 of the 127 PDFs: **`/DCTDecode` 80%** (the stream IS a jpeg — verbatim), CCITT 13%, Flate 3%. With docx also verbatim that is **~82% needing nothing but a byte copy**, and verbatim extraction *cannot* decompression-bomb. 0 multi-page, 1 of 30 multi-image. **Where it runs is settled**: `POST /api/analyse-scorecard` already receives the bytes through multer for OCR, so widen its filter, extract, OCR, and PUT the image server-side — no presigned PUT for documents, and `/sign-s3` is untouched. **Cost is free, not cheap**: 260–325 fixtures a season (~20 document uploads), and Vision at 2 calls per card is 70 units/month against a 1,000 free tier — 14x headroom with *every* card OCR'd. No Vision cache on this side though (the `scorecard-ocr-cache/` objects are all Tameside's), so a retry loop is the only thing that spends units. Phase 1 = verbatim 82%, no new deps, no bomb surface; Flate and CCITT deferred and genuinely optional (13% of 7% is ~3 cards a season). No backfill — forward-facing only. **Phase 1 built: 104 of 141 = 74% of the real corpus**, measured over the WHOLE corpus rather than a sample - all 14 docx, 90 of 127 pdfs, all decodable. Two earlier figures were reported and both were wrong in an instructive direction: 65% was extrapolated from a 40-file sample, 59% was the first honest full-corpus run. **Four bugs in my own code, three of them hidden by the sample**: PDF dictionary keys are unordered (searching only after `/Subtype /Image` missed 4 of 20); an off-by-one in the dict walk (`depth` started at 0 having consumed the closing `>>`, so every walk ran to its guard and returned nothing - with the first, 48% of the sample -> 65%); **`stream` followed by a bare CR** (21 files, one page one image `/DCTDecode`, the exact case Phase 1 exists for, matched nothing because the regex was `/stream\r?\n/` - **59%->74%, the largest single gain, a two-character fix behind a wrong denominator**); and a zip **directory entry** counted as a second image in `fromDocx`, found by a generated fixture within a minute because the bucket's real files don't have one. Also handled, unplanned: `/Filter[/FlateDecode/DCTDecode]`, a jpeg with zlib on top, which Acrobat writes. What still fails is 37 files and mostly **not** codecs any more: CCITT 15, **multi-image page 12** (one scan sliced into 30-89 strips), multi-page 5, no image XObject 3, Flate 2 - so a Phase 2's first job is working out which XObject a page draws, not more decoders. **Fixtures are generated, not real**: the first version used real cards until one was rendered pre-commit and turned out to carry **twelve players' names and both captains' signatures** - not a thing to put in a git repo. Generated turned out better anyway (each file carries one named shape; a real card carries one incidentally) and caught bug 4 immediately. The one real fixture kept is a *blank* form. **Storage half also done**: a decodable document is now stored as an ordinary jpeg (not the pdf - keeping the pdf would preserve the exact thing that is annoying to open), PUT server-side through the same `buildUploadKey` allowlist with no ACL, and its URL comes back in the OCR response for the page to drop into `scoresheet-url` - so the captain uploads once and `/sign-s3` stays images-only. **The form now takes documents on all three photo inputs**, with BOTH upload boxes kept on purpose: the auto-fill box reads the card, and a second endpoint `POST /api/convert-scorecard-document` converts and stores **without OCR**, because some captains would rather no machine read theirs until it has a season behind it (a test asserts `analyseImage` is never called from it). Phase 2 was never the blocker - that is more codecs, this was routing. **And this found the real cause of the reported "scorecard arrived with no photo"**: the plain photo box had no `accept` at all, and its whole error path was `catch (error) { console.error(...) }` - so `/sign-s3` refusing a PDF produced no alert, no message, an empty `scoresheet-url` and a filed scorecard, which matches the Sentry "could not get signed url" exactly. The refusal worked; the page threw it away. All three inputs now share `static/beta/js/scorecard-upload.js`, which either returns a URL or throws a message fit for a captain. Three more bugs fell out: the league form **still rewrote `%20` as `+`** (HARD-03 fixed the other path, not this one), `#scoresheet-img` was assigned `.value` on an `<img>` so the preview never appeared, and the obvious fix for that was *also* wrong - the bucket URL is a broken image since HARD-02b made objects private, which `messer-scorecard.ejs` had already learned and previews from a blob instead. Two unanticipated fixes: the auto-fill input carried **`accept="image/*"`**, so the file dialog never offered a captain their scanner's PDF and the whole server-side path was unreachable from the UI (covered in Playwright - `accept` is a browser behaviour supertest cannot see), and `objectUrl` is now shared with `/sign-s3` because `normalisePhotoUrl` must accept both shapes. **The first test run of this wrote two real objects to the production bucket**: `app.js` calls `dotenv.config()` at import time and every integration test requires it, so the live `.env` - production DATABASE_URL, bucket and AWS credentials - is in every test process. (The DB has not been reached: per-file model mocks plus `saveUninitialized: false` have covered it, which is discipline rather than structure - see HARD-26.) Nothing server-side had ever written to S3 before, so no suite mocked the SDK. `__tests__/setup.js` now plants dummy AWS credentials and closes the credentials file and metadata service, so an unmocked AWS call fails to sign - the Jest counterpart to `e2e/helpers/read-only.js`. **Cap raised 10MB->25MB**: only 5 of 1,494 objects exceed 10MB and **three are genuine scorecards**, so it had been refusing real cards for two seasons. **Zips refused by name.** Two OCR fixes came out of it: the enhance step now encodes to jpeg (a real 13.5MB png reached Vision at 11.8MB; as jpeg it is 2.6MB, **4.6x smaller**) and falls back instead of throwing, which is what made `Parrswood C-Dome B.jpeg` un-OCR-able at any limit - `sharp` cannot decode it and there was no catch. **Closed 12 Sep.** Phase 1 is the part that was ever meant to be built, and the brief's own instruction was to leave 2 and 3 until Phase 1 had a season behind it — so they are now **HARD-29** (`/FlateDecode`, ~1 card a season) and **HARD-30** (`/CCITTFaxDecode`, ~3 a season, and declining to build it is a valid outcome) rather than optional phases buried in a finished package where nobody would see them again. **Exercised in production on 11 Sep**, which it had not been when checked on the 10th: `POST /api/convert-scorecard-document` 200, two `POST /api/analyse-scorecard` 200s and one 400 — the friendly refusal working — and every draft filed since carries a photo, including 2439, which was the one missing its photo before the auto-fill fix went out. |
| [HARD-18](done/HARD-18-migration-runner.md) | **done** | `c51dddf` | Found applying migration 011. **`run-migration.js` splits on `;` with no regard for comments**, so a semicolon in a `--` comment starts a chunk of bare prose — and the real statement can sit behind it. 011 hit exactly this and its `ALTER` would never have run. Loud, not silent, but it reads as a broken migration rather than a broken runner. `db_connect.js`'s `pgify` (commit `baf2215`) already contains the scanner this needs. **Fixed 12 Sep.** The splitter now goes through **`utils/sqlScan.js`**, which is `pgify`'s scanner lifted out of `db_connect.js` — the brief was right that the fix already existed one layer down, and sharing it was better than writing a second state machine that could drift. `pgify` was rewired onto it with no behaviour change, proven by its own 12 tests. **Demonstrated end to end against the local database**, which is the first thing HARD-13 has paid for here: the *original* `011` (recovered from `01e10fe` and kept as a test fixture, so the case is the real one rather than a reconstruction) applies as one statement, and through the old splitter it still fails exactly as reported — three chunks, `syntax error at or near "nothing"`, the `ALTER` stranded behind it. A `$$` function body went from 7 shredded chunks to 2 statements. **Two things beyond the brief.** The runner called `dotenv` directly, so it loaded `.env` only and could *only ever* target production — there was no way to rehearse a migration, which is a strange property for the tool whose failure mode is "the statement never ran". It goes through `tools/lib/loadEnv` now: production by default, `--local` to ask. And its log echoed the first 50 characters of each chunk, which for a migration is the comment paragraph above the statement, telling you nothing about which statement failed; `preview()` reports the first line of real SQL instead. A new test walks **every migration in the repo** and asserts each chunk begins with SQL rather than mid-prose — the 011 signature — so this cannot come back quietly. 32 new tests, each also run through the old splitter to prove it gets them wrong. 1083 Jest. Migration *tracking* stays out of scope and is now **HARD-32**. |
| [HARD-26](done/HARD-26-test-process-carries-production-secrets.md) | **done** | `2210eb7`, `1f3269f` | Found 4 Sep 2026 while building HARD-25's storage half. `app.js` and `instrument.js` call `dotenv.config()` at import time and **31 suites require `app.js`**, so every integration test process holds the real `.env`: production `DATABASE_URL`, `TAMESIDE_DATABASE_URL` (another league's database), `DB_PI_KEY`, the AWS keys. **36 variables in `.env`, 9 overridden in `__tests__/setup.js`** — two of them added that same day. The finding is not any one secret but that this has been patched **three times, symptom by symptom**: Sentry (the comment at `instrument.js:13` names this exact mechanism, and two July `javascript` issues were our own test runs), S3 (**two real objects written to the production bucket** by the first run of the new suite — nothing server-side had ever PUT to S3, so nothing mocked the SDK; the fix also closed SES, meaning `npm test` had held working credentials for the domain that sends the invoices), and Postgres (never breached, and precisely because of per-file model mocks plus `saveUninitialized: false` — **nothing mocks `db_connect`**, and requiring `app.js` opens no connection, so one forgotten mock or one test that touches a session reaches production). `e2e/helpers/read-only.js` already makes the case for enforcing this once rather than trusting each test. Also found: the three `SENDGRID_*` variables are **referenced nowhere** — dead credentials, still live at the provider. **Settle before HARD-08**, which would put all of it into CI. **Fixed 12 Sep.** `app.js` and `instrument.js` skip `dotenv.config()` under `NODE_ENV=test`, so `__tests__/setup.js` **declares** the environment rather than overriding parts of it — options 1 and 3 of the brief, which called them together the real answer. The count was worse than recorded: **40 variables read, 15 declared**, so 25 were inherited including both cron tokens and `SESSION_SECRET`. The interesting half was not the credentials but that **declaring an environment means deciding, per variable, whether absence is meaningful** — and I got it wrong first, breaking three tests by giving fakes to `SNS_TOPIC_ARN` and `AUDIT_EMAIL_FROM`: an unset topic ARN *skips* the check the signature fixtures rely on, and an unset from-address falls back to the league address the digest test asserts. Declaring values made the suite assert things it had itself invented. Those, the cron tokens, the recipient lists and `SENTRY_DSN` are `delete`d, not assigned. `S3_BUCKET_NAME` keeps its **real** value on purpose, as the brief insists — tests compare URLs built from it against `normalisePhotoUrl`, which checks the host against it, so renaming it makes those tests wrong rather than safer. `utils/testEnvGuard.js` is the backstop, and it checks **shapes not names** (a `supabase` host, an `AKIA`/`ASIA` key id, a set cron token) — which is the property that stops it going one variable out of date the way the three previous patches did. Demonstrated as the brief asked: deleting the `DATABASE_URL` line from `setup.js` and supplying a production URL from the shell refuses the run, naming the variable and the host. **The first attempt at that demonstration proved nothing** — `setup.js` deletes the variable I planted before the guard ever sees it, which is worth knowing before writing the next such test. **Closed 12 Sep.** The last criterion — revoke the SendGrid credentials at the provider — turned out to be unreachable and, more usefully, close to moot. The account came from the Heroku add-on and the login went with Heroku in May, so the UI was out; but the credentials could be *probed*, which is the better question. The **SMTP pair is dead** (`535 Bad username / password`) and the **API key still authenticates** — with `sender_verification_eligible` as its only scope and `403` on everything else, so **no `mail.send` and no way to send from the league's domain**. The brief's "dead credentials, still live at the provider" was wrong in both directions at once. Revoking being impossible, the achievable half was done instead: key off Cloud Run (`league-site-00211-227`), all three out of `.env`. Worth keeping: **a credential's state is answerable with the credential itself**, no account needed, and it separates "cannot revoke it" from "it cannot do anything". |
| [HARD-27](done/HARD-27-getemails-sql-construction.md) | **done** | `997266f` | Found 5 Sep 2026 while investigating a Gmail rate-limit bounce. `getEmails` is the query behind every distribution list and builds ~1.5KB of SQL by concatenation. **`DB_PI_KEY` is interpolated as a string literal five times**, once per UNION branch — the pattern CLAUDE.md records as fixed in Aug 2026, missed here — so the decryption key is in the query text and therefore in Cloud Logging, via the `console.log(sql)` on the line above, on every list send. The four `WHERE` terms are interpolated too; **not exploitable today** and the brief says so, because the only caller assigns them from hardcoded arrays after matching the envelope recipient — a loaded gun, not a wound. Also noted: it returns bare email strings with no id, which is exactly why one-click unsubscribe is not possible without changing it. **Fixed 12 Sep.** The key is bound in all five UNION branches and all four WHERE terms are parameterised. Two things worth keeping. **The brief named one function and the acceptance criterion named the file, and the file was right**: `getById` interpolated the key twice and `updateBulk` once more, so the real count was 3 functions and 8 sites, not 1 and 5. `updateBulk` is the interesting one — it used a **template literal**, so the obvious grep (`+ process.env.DB_PI_KEY`) never matched it, and it writes `playerEmail`/`playerTel` on every profile save. A defect found by grep is bounded by the grep. **The old guard was replaced rather than deleted.** `no-sql-logging.test.js` held the weaker rule that was possible while the key was still pasted in ("do not log a statement in a file that interpolates a secret") plus a second assertion whose only job was to confirm the bug still existed — a test that fails when you fix it. It is now `no-secrets-in-sql.test.js`: the key is never interpolated at all, both spellings, with a self-test so it cannot pass by failing to look. That absolute form also catches `updateBulk`, which the old one read as clean for as long as it existed. **Verified against production, not just mocked**: the twelve real distribution lists (every role, two divisions, two clubs, two combinations, and the no-WHERE path) resolve to byte-identical address sets before and after — 310 addresses, all twelve digests unchanged. And one risk was real enough to check rather than assume: `pgp_sym_encrypt(?, ?)` hands Postgres **two untyped parameters**, which can fail overload resolution — so it was proven on the local database, then `updateBulk` was run end to end there (write, read back, restore) to exercise the `?`->`$n` conversion the unit test mocks away. 1051 Jest. **Rotation is deliberately not part of this** — the key was in Cloud Logging for as long as that `console.log` existed, and deferring that by default is itself a decision, so it is now **HARD-31** rather than a line in a closed package. |
| [HARD-14](done/HARD-14-flaky-authorization-test.md) | **superseded 7 Sep** — see the combined HARD-14 + HARD-20 row below | | The brief says the 403→404 fails "roughly one full run in five" in `roster.test.js`. On 7 Sep, a day of work on unrelated packages produced intermittent failures in **five distinct suites** — `scorecard-photo` (socket hang up), `team-withdrawal` (200→404), `roster` (the original), `sign-s3`, `contact-us-club` (8 failures, then 8 passes alone) — every one green in isolation immediately afterwards. About four of a dozen full runs had a failure, so the rate is nearer **one in three**, and it is not an authorization-test problem: the package is named after the first instance found and that name now misleads. It cost real time twice, by reading a flaky full run as a regression from the change just made. **`--runInBand` still passes**, so this is between workers, not within a file — per-file mock hygiene is the wrong place to look. Two candidates: module- registry state shared across workers, and contention on the real resources the suite still touches (HARD-26 — `app.js` calls `dotenv.config()` at import, so all 31 app-requiring suites carry production credentials and a session store pointed at production Postgres; `socket hang up` is the shape of a contended network resource, not of a mock problem). **Settle with HARD-26 before HARD-08** puts the suite in CI. |
| [HARD-14](done/HARD-14-flaky-authorization-test.md) + [HARD-20](done/HARD-20-phantom-401.md) | **done** | `33bac1c` | **One bug, and never our code.** supertest's `app.listen(0)` binds the IPv6 wildcard `::` while dialling 127.0.0.1; the port-0 allocator for `::` hands out ports already held on 127.0.0.1 *specifically*, and loopback delivery then goes to the more specific binding — one of the seven VS Code helpers listening in that range. Binding 127.0.0.1 makes it impossible rather than merely visible, so HARD-20's "the guard cannot prevent the collision" is superseded. Synchronously, via `net._createServerHandle`, because every public host-bearing `listen` form resolves through `dns.lookup` and supertest needs the port in its constructor; falls back to upstream's bind if that internal goes. **The guard was itself wrong**: it inferred "ours" from helmet's CSP / `X-Content-Type-Options`, and Express's own finalhandler 404 carries both — so every collision with an Express squatter was silently attributed to us, which is exactly the "guard silent, so it's ours" residual HARD-14 was chasing. Servers now stamp responses with an id we issue (`__tests__/helpers/foreign-response.js`). **Measured: 5 of 20 runs failed before, 20 of 20 green after** (p ~= 0.003), plus instrumented runs where collision and failing run correlated exactly. Two premises fell with it — `--runInBand` does **not** pass (it makes the same ~616 binds), and the keep-alive/module-registry theories are dead since superagent sets `agent: false`. Not fixed, and not this package's: the **worker-teardown warning**, which fired in 8/20 runs before and 12/20 after with all 20 green — independent of the collisions, and belongs with HARD-26. |
| [HARD-33](done/HARD-33-dev-env-live-credentials.md) | **done** | `3934255` | Found 12 Sep while re-scoping HARD-28. HARD-26 made the Jest process unable to reach anything real; **nothing had done the same for the browser suite**, which starts a REAL server — and that server loaded `dev.env` overlaid on `.env`, so it held a live `AKIA` key. `HeadBucket` on `badmintontemp` returned **200**. The reason `e2e/helpers/read-only.js` did not cover it is the part worth keeping: it intercepts **browser** requests, so it stops a presigned PUT from the page but cannot see a PUT made from inside Node — which is exactly what `POST /api/analyse-scorecard` does with a converted document. A test would pass, the guard would report no writes, and the object would be in the production bucket. `e2e/server-env.js` is now the browser counterpart of `__tests__/setup.js`: local database and key from `dev.env`, dead credentials for everything outbound, the safe-because-unset variables deleted, and it **refuses to start** if anything live survives. Demonstrated by making it try — `PutObject` returns `InvalidAccessKeyId`. `npm run dev` is untouched. **`reuseExistingServer` was the second half**: the suite adopts whatever is on 8080, `npm run prodlocal` included. `/health` now reports `e2e: true` only when our preload set it and `e2e/global-setup.js` refuses without it — checking for OUR marker rather than for evidence of production, so it fails closed. Demonstrated, after a first attempt that proved nothing because a stale server from the previous run was still holding the port and answering with the marker. **And it turned up something nobody had noticed**: the local seed carried **1,479 `scoresheet-url` values pointing at real objects in the production bucket**, so the browser suite had been reading production storage — pictures of team sheets with twelve players' names and two captains' signatures — on every run, unremarked, because it is a same-origin GET. `tools/local-db/sanitise.sql` clears them now, on the same principle that already replaced every email and phone number. 1091 Jest, 71 Playwright. Unblocks HARD-28's submission half. |
| [HARD-28](done/HARD-28-scorecard-e2e-coverage.md) | **done 12 Sep** | `f402d52` | Closed the four gaps the brief left open, and the two bugs writing the tests turned up. **Messer had no gate.** The league card got one in May 2026 (`7c682c8`); messer got the inline feedback and never the wrapper around `sendEvent`, so every rule it stated was advisory — and step 14 derives the match score by comparing `value * 1` per pair, so each unfilled game was silently counted as an **away win**. Its range message also said "between 0 and 30" while the inputs carry `min="-10"` and the server validates -10..30, so the one rule a messer captain cannot guess was the one stated wrongly. Both fixed; the step→games map is now read off the markup because messer's three mixed steps hold ONE game where the other six hold two. **Filing a draft 500'd on a failed email.** `POST /email-scorecard` awaited `mailer.send` bare inside its try AFTER the row was written, so an SES outage sent the captain to the 500 page, whose whole message is that nothing was recorded — and what a captain does about that is file it again. HARD-01's `afterCommit` was in the same file guarding the publish path only; it now lives in `utils/afterCommit.js` and the draft path uses it, redirecting with `notified=0` so the confirmation page can tell "filed and announced" from "filed, and nobody has been told". The messer sends were the mirror image — they caught their own failures and `console.error`'d them, so a failed one was invisible and the approver was answered "Result approved" either way; they report now, as handled Sentry events. **Found by the submission test**, which HARD-33 unblocked: with the suite's dead SES credentials every submission was a 500. New specs: `scorecard-submit.spec.js` (posts the real form, reads the row back, and is the one spec that writes — one `scorecardstore` row per test, declared through `readOnly(..., { allowWrites })`, re-runnable without a reload), `messer-wizard.spec.js`, `scorecard-messages.spec.js`, `scorecard-prefill.spec.js`. It files against a real outstanding fixture rather than an invented pairing, though note that does **not** quiet `--check orphan-drafts` **on the local database**: that check asks whether a *complete* fixture exists, so a filed-and-unpublished draft is exactly what it is built to report. Noise there, not a finding; `local-db.sh load` clears it. 89 Playwright, 1102 Jest. **Not done: gap 6, mobile** — it is in the brief's gap list and not in its acceptance criteria, and captains do file results on phones. Also corrected four e2e comments still saying `dev.env` points at production. |
| [HARD-29](HARD-29-flatedecode-scorecards.md) | not started | | Split out of HARD-25. **3% of PDFs — about one scorecard a season.** `zlib` and `sharp` are both already dependencies, so nothing to install, but unlike Phase 1 there is arithmetic to get right: the raw wrap needs `/Width`, `/Height`, `/BitsPerComponent` and `/ColorSpace` read correctly (HARD-25 measured 3.00 bytes/px on a clean sample — do not assume it), and `/DecodeParms` PNG predictors change how inflated bytes map to pixels. Get that wrong and you get an image that decodes without error and looks like static, which is worse than failing. **This is the phase that introduces the bomb surface**, so the inflate cap is its admission fee. Acceptance says checked *by eye*, because an automated "an image came out" assertion passes on noise. |
| [HARD-30](done/HARD-30-ccitt-scorecards.md) | **done** 21 Sep | | Split out of HARD-25. **13% of PDFs, so ~3 scorecards a season**, and the only case with no tool to hand: `sharp` does not decode CCITT, and ImageMagick delegates PDF work to Ghostscript, which is not installed — HARD-25 made a point of not needing it. Two ways in: a CCITT decoder dependency, or rewrapping the stream as TIFF. **Deciding not to build it is a valid outcome** and better than leaving the question open another year; the current behaviour (store the PDF, skip OCR, tell the captain plainly) is already reasonable for three cards a season. If that is the call, write it down and move this to done/. |
| [HARD-31](done/HARD-31-pi-key-rotation-decision.md) | **done 14 Sep — ROTATED** | | Decided with the league secretary, and recorded with the reasoning, because deferring by default is the failure this package existed to prevent. **The exposure was three log entries** (27 Aug ×2, 1 Sep), readable by **one human account**, with **nothing exporting anywhere**, ageing out 26 Sep / 1 Oct. Rotated anyway: "one human account" describes today's access list and not its history, and the measured cost was 366 values in one transaction with a two-minute cutover at an hour with no active users. **A documented fact turned out to be wrong and is corrected in CLAUDE.md**: Cloud Run request logs retain **30 days, not four months**. That figure came from reading the oldest entry for the service, which is a Cloud *Audit* log in the 400-day `_Required` bucket recording deploys — application logs go to `_Default` at 30 days. It makes "never delete on log silence alone" more important rather than less: a month is a short window, and a season three weeks old is most of it. Live table re-encrypted (167 email, 116 tel, 83 auth); the five archives' ten columns **cleared** rather than re-encrypted, after checking that destroyed nothing unique — every archived contact also exists live, zero that do not. `--check missing-contact` 0 before, 0 after. Cutover via `--update-env-vars` (never `--set-env-vars`), all 28 variables verified, revision `league-site-00232-vts`, confirmed by the secretary loading club contact pages — both contact routes are `secured`, so they cannot be curled anonymously. **The local rehearsal found two bugs in the script's own verification, and that is the part worth keeping.** `pgp_sym_decrypt` with a wrong key RAISES `Wrong key or corrupt data` rather than returning NULL, so the "old key no longer works" check aborted the transaction it sat inside — rolling the entire rotation back at the final step, after every column had already succeeded — and the "pre-existing mixed-key mess" guard compared two counts that can never differ. Run blind at production, the first would have looked like an unexplained failure with the operator holding a key the database had never been given. A rotation script that cannot be rehearsed is one that gets run blind. |
| [HARD-32](HARD-32-migration-tracking.md) | not started | | Noted as out of scope in HARD-18 and split out 12 Sep 2026. **There is no `schema_migrations` table**, so nothing records which of the numbered migrations a given database has seen — the question you actually want answered before a deploy is "has this one run against production yet?", and today it is answered by remembering. Low severity because the files are written with `IF NOT EXISTS`, `run-migration.js` treats `already exists` as a skip, and `tools/local-db.sh load` replays from nothing — so the local database is defined by the files rather than by a history. **This package had no status row at all until 15 Sep**, which is what the rewritten guard in `__tests__/unit/docs-references.test.js` found on its first run: the old one accepted a conflict-map row as a status row. |
| [HARD-34](HARD-34-scorecard-on-a-phone.md) | not started | | HARD-28's gap 6, the one item in its gap list that its acceptance criteria did not cover — recorded rather than quietly dropped. **Captains file results on a phone, in a sports hall, at ten at night**, and nothing tests that: HARD-28's work asserts what the form submits and what the captain is told, both at desktop width only. The precedent is `roster-edit.spec.js`, whose phone coverage exists because the roster editor bound `dragstart`/`drop` — events mobile browsers never fire from touch — so **nothing on that page worked on a phone and no test could see it**. The scorecard wizard is the harder target: fourteen steps in a modal, thirty-six number inputs, and a gate whose `#gameFeedbackN` may land below the fold, which makes a blocked Continue look like a dead button. |
| [HARD-35](HARD-35-invoice-run-reporting.md) | not started | | HARD-23's step 5 — in its *What to do* and not in its acceptance criteria, so recorded rather than dropped, exactly as HARD-28's gap 6 became HARD-34. **HARD-23 removed two of the three things that made the 2026 invoice failure last a year** (a 302 Make.com read as success; a 200 whose body was the error) and not the third: **nothing watches whether a scheduled run happened at all.** A run that now fails correctly still fails into silence — Make.com records a red step in a history nobody reads, and the next signal is a treasurer asking. Generalises past invoices to the registration reminder and the audit digest itself. Note any new digest line must escalate rather than be collapsed by HARD-07's `TRACKED` baseline, or it will be added and never seen. |
| *(finding, resolved same day)* | **done 14 Sep** | | **34 players were invisible to the player search, and are not any more.** `models/players.js:317` builds it as `player JOIN team ON team.id = player.team` — inner — and 34 players pointed at a team id that no longer exists (teams 34, 21, 15, holding 19 / 8 / 7). Demonstrated at the time: `SELECT count(*) FROM player JOIN team ON team.id = player.team WHERE player.team = 34` returned **0** while those players plainly existed. Same family as HARD-11 — orphaned references, no foreign keys — but on `player.team`, which HARD-11's brief does not survey. Owner's call: park them on the **No Club / No Team sentinels** rather than hunt the deleted teams. Done through `Roster.releasePlayer`, the editor's own path, one transaction per player — it survives a deleted team because `getPlayerOwner` uses `LEFT JOIN team`, checked rather than assumed, since an inner join there would have thrown "No such player" for every one of them. **The search invisibility is fixed as a side effect**, which is the better half: team 52 exists, so the inner join reaches them. Verified 0 → 34 found. What was deliberately given up is the club association (Macclesfield for 19, Disley for 7; team 21's were already NULL) and 14 nominated ranks, all now reserves on the sentinel — which is what "released" means here. `--check all` unchanged. The underlying cause — no foreign key on `player.team` — is untouched and still HARD-11's. |
| *(finding, no package yet)* | — | | **`isSuperAdmin` is defined twice** — `utils/authz.js` and `middleware/requireClubAccess.js` — with the same behaviour and no relationship between them. Found while building `requireCronCaller` (HARD-23), which needed one of them; `requirePublishAuthority` (HARD-24) happened to pick the other. Two definitions of a security predicate is one more than is safe: a change to either is invisible from the other. Left alone rather than fixed inside HARD-23, per the rules of engagement. The fix is small — have `requireClubAccess` import from `utils/authz` — but it touches every authorization path, so it wants its own change and its own test run. |
| *(finding, resolved same day)* | **done 15 Sep** | | **The weekly Instagram carousel cannot ever have been landing, and `GET /fixture/outstanding` was an unauthenticated email trigger.** Both found surveying what Make.com actually does for this league, which turned out to be a better audit than looking at our own code: the scenario named "late scorecards" contains a single HTTP module pointed at that route, and **an endpoint is often ungated precisely because the automation calling it cannot present a credential**. Now on `requireCronCaller`, its fourth caller (`LATE_SCORECARD_CRON_TOKEN`). The carousel had two independent faults, both true since it was built: the images are **PNG and Instagram accepts JPEG only**, and they are files in `static/beta/images/generated/` — the container's own disk, which on Cloud Run belongs to one instance and does not outlive it. All four table URLs answered **404** when checked. Facebook was unaffected and still works, because Make fetches the bytes itself and uploads them as `data` rather than handing over a URL — **one platform posting and the other silently not is what a format constraint on the fetching side looks like**. Fixed with `/league-table-image/:division` and `/tournament-image/:poster`, generated per request and returned as JPEG, the shape `/resultImage` already used. Also fixed, one layer down: `utils/testEnvGuard.js` matched cron tokens against a **hardcoded list of three names**, which is the exact drift its own header claims shape-matching avoids — and it had already gone stale, never having been given `INVOICE_CRON_TOKEN`. Now any `*_CRON_TOKEN`. |
| [HARD-36](done/HARD-36-failed-scorecard-analysis-is-unfalsifiable.md) | **done** 17 Sep | | Found 16 Sep when a captain's first two scorecard photos failed analysis and the third worked. `POST /api/analyse-scorecard` reads the bytes and **discards them** unless the analysis succeeds, so the only artefacts of a failure are a log line and a request size — and diagnosing it meant working from the one image that had, by definition, succeeded. An hour of Vision calls established that `DATE` was the tightest anchor on the card by a distance (0.033 of headroom against 0.30, where every other anchor had 0.125–0.170) and that finding was worth having, but it did **not** establish what was different about the two failures and could not have. `a44d16b` widened the band and made the message distinguish *we refused it* from *it was not there*; **without the image even a perfect message cannot be checked**. Store failures under their own prefix with a 14-day lifecycle expiry. **S3 versioning was considered and is the wrong tool** — it protects an object that exists, and here none is ever created; it also retains *more*, indefinitely, which is the opposite of what a photo of twelve players' names and two signatures wants. **Landed 16 Sep — and the first real failure went straight through it on 17 Sep.** `storeFailedImage` hung off `analyse_scorecard`'s CATCH, but the 4xx refusals return from inside the try and never reach it; three branches in `convert_scorecard_document` returned 400 and none of them logged which one fired. Worse, documents were exempted on the reasoning that `convertDocument` stores the image before the OCR — true when extraction SUCCEEDS, and the exemption was therefore widest exactly where evidence was scarcest. `refuseUpload` now keeps the upload as it arrived (the pdf/docx, since there is no image) and names the check that refused it. `FAILED_UPLOAD_TYPES` is deliberately separate from `ALLOWED_TYPES`, which guards the attacker-chosen content type on `/sign-s3`. Lifecycle rule verified live on the bucket. |
| [HARD-37](HARD-37-social-card-artwork.md) | **code done, one decision open** | | Raised by the owner on 16 Sep while looking at the first weekly-fixtures cards. The four division backgrounds have two 2024 layout decisions baked into the pixels: a **fade to near-white across the bottom third** (so `resultImage` had somewhere light to put its bottom-right text) and the **division marker `P`/`1`/`2`/`3` drawn into the image**. Both are inherited by anything drawn on them since — the fixtures card's panel sits over the fade so the division colour dies off down the picture, and a card that also *names* the division prints the same thing twice a few hundred pixels apart. Neither is a bug; both are one layout's requirements frozen into the only copy of the artwork. **The cost is that the artwork cannot be composed with.** Owner has the originals and will supply clean full-bleed versions with no text. `fixturesBackground()` is already the single lookup, so re-pointing is one path change. **UNBLOCKED 22 Sep 2026**: four clean backgrounds now exist in `tools/artwork/assets/backgrounds/`, two recovered from the GIMP document and two generated by `tools/artwork/`. The package's premise was also half wrong — the backgrounds were never photographs, they are generated low-poly gradients, so subject/background/letter/fade were always separable. **LANDED 22 Sep 2026**: clean backgrounds in `static/beta/images/bg/divisions/`, the division letter and the panel drawn at render time via `utils/socialCard.js`, and both cards redrawn as siblings with the de-identified player composited back on. Re-pointing the background and redesigning the result card turned out to be ONE job: the old card was black text legible only because the artwork faded to near-white exactly where it sat. **Still open:** whether all four backgrounds are regenerated so the set reads as one league. **The actual prize was the result card** — reviewing it, which goes out every time a captain publishes, still uses the 2024 layout the fade was made for, and carries neither the league's name nor any clue what competition it is. That review needs the clean artwork first, which is why it lives here. |
| *(review of this file, resolved same day)* | **done 15 Sep** | | **This README had gone stale in a dozen places, and `docs-references.test.js` was structurally unable to see almost all of them.** The old check matched `^\|\s*\[(HARD-\d+)\]` anywhere in the file, so it could not tell the conflict map from the status table and could not see a row written without a link — which is the spelling every stale row was in. Found and fixed: `HARD-11 \| not started` still standing after HARD-11 landed; three HARD-14 rows and two HARD-20 rows where only the last was current; HARD-12's status cell still claiming a prerequisite outstanding four days after the flip; HARD-09's "the three left are correct" reading as present tense when `--check all` is now 2 of 14 and `orphan-team-refs` is 0; a **blank line mid-table** that silently ended the table above HARD-31, dropping the last five entries out of it; a conflict-map row pasted into the status table, so HARD-33 had two status rows and no map row; **HARD-28 absent from the conflict map entirely**; three rows with an update appended as a fifth cell and two that never closed their last cell, all five shifting every column after the mistake; nine conflict-map rows carrying `done` in the **Blocked by** column, which is the mechanism by which it came to contradict the status table; and rule 3's test count, stale for the third time. **The rewritten guard then found one more on its first run: HARD-32 had no status row at all**, having only ever had a conflict-map one. Each new assertion was verified by reintroducing the fault and watching it fail. The lesson is the file's own: a guard that checks a weaker property than its header claims is the same failure as a warning that has quietly inverted — its header claimed it checked "the backlog and its status table", and it checked neither table specifically. |
